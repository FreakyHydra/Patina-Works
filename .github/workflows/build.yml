name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build Patina Works
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Compile, test, and build JAR
        run: ./gradlew build --no-daemon --stacktrace --info --refresh-dependencies --console=plain

      - name: Upload built JAR
        uses: actions/upload-artifact@v7
        with:
          name: patinaworks-${{ github.sha }}
          path: build/libs/*.jar
          if-no-files-found: error
          retention-days: 14
